provider:
  name: faas
  gateway: http://127.0.0.1:8080

functions:
  bot-revision-process:
    lang: csharp
    handler: ./bot-revision-process
    image: ngcdev/ci:bot-revision-process
    environment:
      content_type: application/json
      write_debug: true

